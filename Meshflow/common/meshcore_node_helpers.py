"""MeshCore-specific node identity and wire helpers.

Phase 1.0 reserves this module so MC helpers live next to `mesh_node_helpers` (Meshtastic).
Implement real helpers when MeshCore identity columns land (see meshflow-api#279).
"""
